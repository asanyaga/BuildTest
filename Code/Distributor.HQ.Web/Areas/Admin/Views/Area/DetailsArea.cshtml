@model Distributr.HQ.Lib.ViewModels.Admin.CostCenter.AreaViewModel

@{
    ViewBag.Title = "DetailsArea";
}

<h2>DetailsArea</h2>

<fieldset>
    <legend>AreaViewModel</legend>

    <div class="display-label">Name</div>
    <div class="display-field">@Model.Name</div>

    <div class="display-label">Description</div>
    <div class="display-field">@Model.Description</div>

  
</fieldset>
<p>
    @Html.ActionLink("Edit", "EditArea", new { id=Model.Id }) |
    @Html.ActionLink("Back to List", "Index")
</p>

